// Ambient declarations for packages without their own type definitions.
// Global script (no import/export) so `declare module` registers as ambient.
declare module "youtube-search-api";
declare module "discord.js-pagination";
